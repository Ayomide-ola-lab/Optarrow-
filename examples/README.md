# Examples

High-level examples are organized by interface to keep growth simple and consistent.

- `matlab_interface/`: MATLAB-facing usage and integration tests.
- `python_interface/`: reserved for future Python-facing examples.

Current entry point:

- `matlab_interface/minimal_optarrow_test.m`
